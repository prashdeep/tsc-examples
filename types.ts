let numberType:number;
let strType:string;
let booleanType:boolean;
let arrayType:any[];
let initializeArray:number[]=[1,2,3,4];

enum Options{DEPOSIT, CHECK_BALANCE, WITHDRAW};

let option:Options= Options.CHECK_BALANCE;